<div class="container">
    {{> header}}
        <a href="#/create" class="btn btn-default">Create Team</a>
    <h1>Catalog Page</h1>
    <div>
        {{#each teams}}
            {{> team}}
        {{/each}}
    </div>
</div>

{{> footer}}